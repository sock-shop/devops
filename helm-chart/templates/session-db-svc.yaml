---
apiVersion: v1
kind: Service
metadata:
  name: session-db
  labels:
    name: session-db
  namespace: {{ .Values.namespace }}
spec:
  ports:
    # the port that this service should serve on
    - port: {{ .Values.session_db.containerPort }}
      targetPort: {{ .Values.session_db.containerPort }}
  selector:
    name: session-db
