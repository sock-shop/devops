---
apiVersion: v1
kind: Service
metadata:
  name: queue-master
  annotations:
    prometheus.io/scrape: 'true'
  labels:
    name: queue-master
  namespace: {{ .Values.namespace }}
spec:
  ports:
    # the port that this service should serve on
    - port: {{ .Values.queue_master.containerPort }}
      targetPort: {{ .Values.queue_master.containerPort }}
  selector:
    name: queue-master